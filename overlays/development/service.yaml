apiVersion: v1
kind: Service
metadata:
  name: frankdev-service
  namespace: frankdev
spec:
  selector: 
    app: frankdev-nuxtjs
  type: ClusterIP  
  ports:
    - port: 80
      nodePort: 30080
      targetPort: 80
  internalTrafficPolicy: Local